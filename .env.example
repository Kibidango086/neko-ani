

# Backend API Configuration
# For development, the frontend will proxy to http://localhost:3001
# For production, ensure the backend is deployed to the same domain or configure appropriately
VITE_API_URL=http://localhost:3001/api
