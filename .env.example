# Backend API Configuration
# In Vercel, this should be relative '/api' or the full deployment URL
VITE_API_URL=/api

# Remote Video Extraction Server
# The URL of the external service that handles video extraction
# Example: https://extraction-service.example.com/api/extract-video
REMOTE_EXTRACT_API_URL=https://example.com/api/extract-video