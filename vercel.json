{
  "version": 2,
  
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1.ts" },
    { "src": "/(.*)", "dest": "/$1" }
  ],
  "buildCommand": "npm run vercel-build",
  "outputDirectory": "dist",
  
}
